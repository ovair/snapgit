version: 2

project_name: snapgit

builds:
  - main: ./cmd/sg
    binary: sg
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X snapgit/internal/cli.Version={{ .Version }}

archives:
  - format: tar.gz
    name_template: "sg_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
